# Code Review: F2+F3+F4 Replay Review Defect Fixes

**Date**: 2026-04-10
**Scope**: diff -- 13 files reviewed (re-review after F2/F3/F4 fixes)
**Reviewer**: /forge-review (4 subagents)

---

## Executive Summary

0 critical, 0 warnings, 5 suggestions across 13 files. All 3 targeted defects from the prior review are confirmed resolved: circular import broken (F2), milestone checks rate-invariant (F3), store writes batched atomically (F4). No new regressions introduced. The remaining suggestions are pre-existing optimization opportunities carried forward from the prior review.

**Verdict: LGTM** -- all blocking issues resolved.

---

## Critical Findings

No critical findings.

---

## Warnings

No warnings.

---

## Prior Findings Status

| Prior Finding | Status | Verification |
|---------------|--------|-------------|
| P1 (circular import) | **RESOLVED** | No import from useAlerts in mission-store.ts. setTimeMode is pure. |
| W1/C5 (milestone 30s interval) | **RESOLVED** | setInterval gone. Range-based subscribe detection confirmed. |
| W2/S2 (clock drift at 10,000x) | **RESOLVED** | Single atomic setState replaces 3 separate set() calls. |
| S1 (tickSimulatedTime dead code) | **RESOLVED** | Removed from interface and implementation. |
| E2 (3 set() calls per tick) | **RESOLVED** | Single useMissionStore.setState() per tick. |

---

## Suggestions

### S1: Trajectory useMemo recomputes 3,239 vectors at 4Hz (carried forward)

- **File**: `src/components/Trajectory.tsx:57-94`
- **Agent**: Complexity Analyst
- **Issue**: Pre-existing from prior review. Quantize simEpochMs to OEM segment boundary to reduce recomputation.

### S2: moonDist uses previous tick's moonPosition (new, minor)

- **File**: `src/components/DataDriver.tsx:70-76`
- **Agent**: Efficiency Sentinel
- **Issue**: `moonDist` reads `store.moonPosition` (previous tick) while the fresh moon position is computed at line 79. One-tick lag = ~250m at Moon's velocity — imperceptible. Fix: compute moonDist from the freshly computed `moonPos` array.

### S3: Module-level subscribe in useAlerts never unsubscribes (new, acceptable)

- **File**: `src/hooks/useAlerts.ts:23-29`
- **Agent**: Complexity Analyst, Efficiency Sentinel
- **Issue**: Subscribe runs at import time, persists for app lifetime. Correct for SPA; minor HMR/test concern.

### S4: Clamping logic duplicated (carried forward, partially resolved)

- **File**: `src/store/mission-store.ts:156`, `src/components/DataDriver.tsx:34-36,90-93`
- **Agent**: Complexity Analyst
- **Issue**: Clamping in setSimTime and in DataDriver's batched setState. tickSimulatedTime (third location) was removed.

### S5: URL subscribe fires on all store changes (carried forward)

- **File**: `src/hooks/useTimeControlInit.ts:35`
- **Agent**: Efficiency Sentinel
- **Issue**: Pre-existing from prior review. Use selector-based subscribe.

---

## Subagent Reports

### Complexity Analyst
Prior C5 (milestone interval) RESOLVED. Prior C1 (Trajectory 4Hz) carried forward. New: S1 (moon double-division, pre-existing pattern), S2 (module-level subscribe).

### DS&A Reviewer
All 3 prior findings (S1 dead code, S2 drift, S5 milestone) RESOLVED. No new findings.

### Paradigm Enforcer
Prior P1 (circular import) RESOLVED. setTimeMode is pure. No new paradigm violations. Clean.

### Efficiency Sentinel
Prior E2 (3 set() calls) RESOLVED. New: S1 (moonDist stale by 1 tick), S2 (module-level subscribe). Batching implementation verified correct.
